rootProject.name = 'firebase_core'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files(rootDir.toPath().resolve("gradle/libs.versions.toml")))
        }
    }
}
