name: firebase_in_app_messaging_example
description: Demonstrates how to use the firebase_in_app_messaging plugin.
publish_to: "none"

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  firebase_analytics: ^10.1.5
  firebase_core: ^2.7.1
  firebase_in_app_messaging: ^0.7.0+15
  firebase_in_app_messaging_platform_interface: ^0.2.1+34
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
