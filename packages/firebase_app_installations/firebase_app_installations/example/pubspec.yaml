name: firebase_app_installations_example
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1
resolution: workspace

environment:
  sdk: '^3.6.0'
  flutter: '>=3.27.0'

dependencies:
  firebase_core: ^4.8.0
  firebase_app_installations: ^0.4.2+1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
