specVersion: "v1alpha"
serviceId: "dataconnect"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "dataconnect"
      cloudSql:
        instanceId: "fdc-test-north1"
connectorDirs: ["./connector"]
