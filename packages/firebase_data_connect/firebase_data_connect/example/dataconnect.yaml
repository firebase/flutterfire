specVersion: "v1alpha"
serviceId: "dataconnect"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "dataconnect-test"
      cloudSql:
        instanceId: "local"
connectorDirs: ["./connector"]
