// Copyright 2025, the Chromium project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.
// Autogenerated from Pigeon (v25.3.2), do not edit directly.
// See also: https://pub.dev/packages/pigeon

#ifndef PIGEON_MESSAGES_G_H_
#define PIGEON_MESSAGES_G_H_
#include <flutter/basic_message_channel.h>
#include <flutter/binary_messenger.h>
#include <flutter/encodable_value.h>
#include <flutter/standard_message_codec.h>

#include <map>
#include <optional>
#include <string>

namespace firebase_crashlytics_windows {

// Generated class from Pigeon.

class FlutterError {
 public:
  explicit FlutterError(const std::string& code) : code_(code) {}
  explicit FlutterError(const std::string& code, const std::string& message)
      : code_(code), message_(message) {}
  explicit FlutterError(const std::string& code, const std::string& message,
                        const flutter::EncodableValue& details)
      : code_(code), message_(message), details_(details) {}

  const std::string& code() const { return code_; }
  const std::string& message() const { return message_; }
  const flutter::EncodableValue& details() const { return details_; }

 private:
  std::string code_;
  std::string message_;
  flutter::EncodableValue details_;
};

template <class T>
class ErrorOr {
 public:
  ErrorOr(const T& rhs) : v_(rhs) {}
  ErrorOr(const T&& rhs) : v_(std::move(rhs)) {}
  ErrorOr(const FlutterError& rhs) : v_(rhs) {}
  ErrorOr(const FlutterError&& rhs) : v_(std::move(rhs)) {}

  bool has_error() const { return std::holds_alternative<FlutterError>(v_); }
  const T& value() const { return std::get<T>(v_); };
  const FlutterError& error() const { return std::get<FlutterError>(v_); };

 private:
  friend class CrashlyticsHostApi;
  ErrorOr() = default;
  T TakeValue() && { return std::get<T>(std::move(v_)); }

  std::variant<T, FlutterError> v_;
};

class PigeonInternalCodecSerializer : public flutter::StandardCodecSerializer {
 public:
  PigeonInternalCodecSerializer();
  inline static PigeonInternalCodecSerializer& GetInstance() {
    static PigeonInternalCodecSerializer sInstance;
    return sInstance;
  }

  void WriteValue(const flutter::EncodableValue& value,
                  flutter::ByteStreamWriter* stream) const override;

 protected:
  flutter::EncodableValue ReadValueOfType(
      uint8_t type, flutter::ByteStreamReader* stream) const override;
};

// Represents all methods for the Firebase Crashlytics plugin.
//
// Generated interface from Pigeon that represents a handler of messages from
// Flutter.
class CrashlyticsHostApi {
 public:
  CrashlyticsHostApi(const CrashlyticsHostApi&) = delete;
  CrashlyticsHostApi& operator=(const CrashlyticsHostApi&) = delete;
  virtual ~CrashlyticsHostApi() {}
  // Records a non-fatal error.
  virtual void RecordError(
      const flutter::EncodableMap& arguments,
      std::function<void(std::optional<FlutterError> reply)> result) = 0;
  // Sets a custom key-value pair.
  virtual void SetCustomKey(
      const flutter::EncodableMap& arguments,
      std::function<void(std::optional<FlutterError> reply)> result) = 0;
  // Sets the user identifier.
  virtual void SetUserIdentifier(
      const flutter::EncodableMap& arguments,
      std::function<void(std::optional<FlutterError> reply)> result) = 0;
  // Logs a message.
  virtual void Log(
      const flutter::EncodableMap& arguments,
      std::function<void(std::optional<FlutterError> reply)> result) = 0;
  // Enables/disables automatic data collection.
  virtual void SetCrashlyticsCollectionEnabled(
      const flutter::EncodableMap& arguments,
      std::function<void(ErrorOr<std::optional<flutter::EncodableMap>> reply)>
          result) = 0;
  // Check for unsent reports.
  virtual void CheckForUnsentReports(
      std::function<void(ErrorOr<flutter::EncodableMap> reply)> result) = 0;
  // Send any unsent reports.
  virtual void SendUnsentReports(
      std::function<void(std::optional<FlutterError> reply)> result) = 0;
  // Delete any unsent reports.
  virtual void DeleteUnsentReports(
      std::function<void(std::optional<FlutterError> reply)> result) = 0;
  // Check if app crashed on previous execution.
  virtual void DidCrashOnPreviousExecution(
      std::function<void(ErrorOr<flutter::EncodableMap> reply)> result) = 0;
  // Force a crash for testing.
  virtual void Crash(
      std::function<void(std::optional<FlutterError> reply)> result) = 0;

  // The codec used by CrashlyticsHostApi.
  static const flutter::StandardMessageCodec& GetCodec();
  // Sets up an instance of `CrashlyticsHostApi` to handle messages through the
  // `binary_messenger`.
  static void SetUp(flutter::BinaryMessenger* binary_messenger,
                    CrashlyticsHostApi* api);
  static void SetUp(flutter::BinaryMessenger* binary_messenger,
                    CrashlyticsHostApi* api,
                    const std::string& message_channel_suffix);
  static flutter::EncodableValue WrapError(std::string_view error_message);
  static flutter::EncodableValue WrapError(const FlutterError& error);

 protected:
  CrashlyticsHostApi() = default;
};
}  // namespace firebase_crashlytics_windows
#endif  // PIGEON_MESSAGES_G_H_
