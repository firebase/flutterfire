// Copyright 2026, the Chromium project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.
// Autogenerated from Pigeon (v26.3.4), do not edit directly.
// See also: https://pub.dev/packages/pigeon

import Foundation

#if os(iOS)
  import Flutter
#elseif os(macOS)
  import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class PigeonError: Error {
  let code: String
  let message: String?
  let details: Sendable?

  init(code: String, message: String?, details: Sendable?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    "PigeonError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>")"
  }
}

private func wrapResult(_ result: Any?) -> [Any?] {
  [result]
}

private func wrapError(_ error: Any) -> [Any?] {
  if let pigeonError = error as? PigeonError {
    return [
      pigeonError.code,
      pigeonError.message,
      pigeonError.details,
    ]
  }
  if let flutterError = error as? FlutterError {
    return [
      flutterError.code,
      flutterError.message,
      flutterError.details,
    ]
  }
  return [
    "\(error)",
    "\(Swift.type(of: error))",
    "Stacktrace: \(Thread.callStackSymbols)",
  ]
}

private func isNullish(_ value: Any?) -> Bool {
  value is NSNull || value == nil
}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}

private func doubleEqualsFirebaseCrashlyticsMessages(_ lhs: Double, _ rhs: Double) -> Bool {
  (lhs.isNaN && rhs.isNaN) || lhs == rhs
}

private func doubleHashFirebaseCrashlyticsMessages(_ value: Double, _ hasher: inout Hasher) {
  if value.isNaN {
    hasher.combine(0x7FF8_0000_0000_0000)
  } else {
    // Normalize -0.0 to 0.0
    hasher.combine(value == 0 ? 0 : value)
  }
}

func deepEqualsFirebaseCrashlyticsMessages(_ lhs: Any?, _ rhs: Any?) -> Bool {
  let cleanLhs = nilOrValue(lhs) as Any?
  let cleanRhs = nilOrValue(rhs) as Any?
  switch (cleanLhs, cleanRhs) {
  case (nil, nil):
    return true

  case (nil, _), (_, nil):
    return false

  case (let lhs as AnyObject, let rhs as AnyObject) where lhs === rhs:
    return true

  case is (Void, Void):
    return true

  case (let lhsArray, let rhsArray) as ([Any?], [Any?]):
    guard lhsArray.count == rhsArray.count else { return false }
    for (index, element) in lhsArray.enumerated() {
      if !deepEqualsFirebaseCrashlyticsMessages(element, rhsArray[index]) {
        return false
      }
    }
    return true

  case (let lhsArray, let rhsArray) as ([Double], [Double]):
    guard lhsArray.count == rhsArray.count else { return false }
    for (index, element) in lhsArray.enumerated() {
      if !doubleEqualsFirebaseCrashlyticsMessages(element, rhsArray[index]) {
        return false
      }
    }
    return true

  case (let lhsDictionary, let rhsDictionary) as ([AnyHashable: Any?], [AnyHashable: Any?]):
    guard lhsDictionary.count == rhsDictionary.count else { return false }
    for (lhsKey, lhsValue) in lhsDictionary {
      var found = false
      for (rhsKey, rhsValue) in rhsDictionary {
        if deepEqualsFirebaseCrashlyticsMessages(lhsKey, rhsKey) {
          if deepEqualsFirebaseCrashlyticsMessages(lhsValue, rhsValue) {
            found = true
            break
          } else {
            return false
          }
        }
      }
      if !found { return false }
    }
    return true

  case (let lhs as Double, let rhs as Double):
    return doubleEqualsFirebaseCrashlyticsMessages(lhs, rhs)

  case (let lhsHashable, let rhsHashable) as (AnyHashable, AnyHashable):
    return lhsHashable == rhsHashable

  default:
    return false
  }
}

func deepHashFirebaseCrashlyticsMessages(value: Any?, hasher: inout Hasher) {
  let cleanValue = nilOrValue(value) as Any?
  if let cleanValue {
    if let doubleValue = cleanValue as? Double {
      doubleHashFirebaseCrashlyticsMessages(doubleValue, &hasher)
    } else if let valueList = cleanValue as? [Any?] {
      for item in valueList {
        deepHashFirebaseCrashlyticsMessages(value: item, hasher: &hasher)
      }
    } else if let valueList = cleanValue as? [Double] {
      for item in valueList {
        doubleHashFirebaseCrashlyticsMessages(item, &hasher)
      }
    } else if let valueDict = cleanValue as? [AnyHashable: Any?] {
      var result = 0
      for (key, value) in valueDict {
        var entryKeyHasher = Hasher()
        deepHashFirebaseCrashlyticsMessages(value: key, hasher: &entryKeyHasher)
        var entryValueHasher = Hasher()
        deepHashFirebaseCrashlyticsMessages(value: value, hasher: &entryValueHasher)
        result = result &+ ((entryKeyHasher.finalize() &* 31) ^ entryValueHasher.finalize())
      }
      hasher.combine(result)
    } else if let hashableValue = cleanValue as? AnyHashable {
      hasher.combine(hashableValue)
    } else {
      hasher.combine(String(describing: cleanValue))
    }
  } else {
    hasher.combine(0)
  }
}

/// Generated class from Pigeon that represents data sent in messages.
struct CrashlyticsStackFrame: Hashable {
  /// Dart reserved word `class`; native maps this to the historical "class" key.
  var className: String?
  var method: String
  var file: String
  var line: String

  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> CrashlyticsStackFrame? {
    let className: String? = nilOrValue(pigeonVar_list[0])
    let method = pigeonVar_list[1] as! String
    let file = pigeonVar_list[2] as! String
    let line = pigeonVar_list[3] as! String

    return CrashlyticsStackFrame(
      className: className,
      method: method,
      file: file,
      line: line
    )
  }

  func toList() -> [Any?] {
    [
      className,
      method,
      file,
      line,
    ]
  }

  static func == (lhs: CrashlyticsStackFrame, rhs: CrashlyticsStackFrame) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return deepEqualsFirebaseCrashlyticsMessages(lhs.className, rhs.className)
      && deepEqualsFirebaseCrashlyticsMessages(
        lhs.method,
        rhs.method
      ) && deepEqualsFirebaseCrashlyticsMessages(lhs.file, rhs.file)
      && deepEqualsFirebaseCrashlyticsMessages(
        lhs.line,
        rhs.line
      )
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("CrashlyticsStackFrame")
    deepHashFirebaseCrashlyticsMessages(value: className, hasher: &hasher)
    deepHashFirebaseCrashlyticsMessages(value: method, hasher: &hasher)
    deepHashFirebaseCrashlyticsMessages(value: file, hasher: &hasher)
    deepHashFirebaseCrashlyticsMessages(value: line, hasher: &hasher)
  }
}

/// Generated class from Pigeon that represents data sent in messages.
struct RecordErrorRequest: Hashable {
  var exception: String
  var information: String
  var reason: String?
  var fatal: Bool
  var buildId: String
  var loadingUnits: [String]
  var stackTraceElements: [CrashlyticsStackFrame]

  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> RecordErrorRequest? {
    let exception = pigeonVar_list[0] as! String
    let information = pigeonVar_list[1] as! String
    let reason: String? = nilOrValue(pigeonVar_list[2])
    let fatal = pigeonVar_list[3] as! Bool
    let buildId = pigeonVar_list[4] as! String
    let loadingUnits = pigeonVar_list[5] as! [String]
    let stackTraceElements = pigeonVar_list[6] as! [CrashlyticsStackFrame]

    return RecordErrorRequest(
      exception: exception,
      information: information,
      reason: reason,
      fatal: fatal,
      buildId: buildId,
      loadingUnits: loadingUnits,
      stackTraceElements: stackTraceElements
    )
  }

  func toList() -> [Any?] {
    [
      exception,
      information,
      reason,
      fatal,
      buildId,
      loadingUnits,
      stackTraceElements,
    ]
  }

  static func == (lhs: RecordErrorRequest, rhs: RecordErrorRequest) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return deepEqualsFirebaseCrashlyticsMessages(lhs.exception, rhs.exception)
      && deepEqualsFirebaseCrashlyticsMessages(
        lhs.information,
        rhs.information
      ) && deepEqualsFirebaseCrashlyticsMessages(lhs.reason, rhs.reason)
      && deepEqualsFirebaseCrashlyticsMessages(
        lhs.fatal,
        rhs.fatal
      ) && deepEqualsFirebaseCrashlyticsMessages(lhs.buildId, rhs.buildId)
      && deepEqualsFirebaseCrashlyticsMessages(
        lhs.loadingUnits,
        rhs.loadingUnits
      ) && deepEqualsFirebaseCrashlyticsMessages(lhs.stackTraceElements, rhs.stackTraceElements)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("RecordErrorRequest")
    deepHashFirebaseCrashlyticsMessages(value: exception, hasher: &hasher)
    deepHashFirebaseCrashlyticsMessages(value: information, hasher: &hasher)
    deepHashFirebaseCrashlyticsMessages(value: reason, hasher: &hasher)
    deepHashFirebaseCrashlyticsMessages(value: fatal, hasher: &hasher)
    deepHashFirebaseCrashlyticsMessages(value: buildId, hasher: &hasher)
    deepHashFirebaseCrashlyticsMessages(value: loadingUnits, hasher: &hasher)
    deepHashFirebaseCrashlyticsMessages(value: stackTraceElements, hasher: &hasher)
  }
}

private class FirebaseCrashlyticsMessagesPigeonCodecReader: FlutterStandardReader {
  override func readValue(ofType type: UInt8) -> Any? {
    switch type {
    case 129:
      return CrashlyticsStackFrame.fromList(readValue() as! [Any?])
    case 130:
      return RecordErrorRequest.fromList(readValue() as! [Any?])
    default:
      return super.readValue(ofType: type)
    }
  }
}

private class FirebaseCrashlyticsMessagesPigeonCodecWriter: FlutterStandardWriter {
  override func writeValue(_ value: Any) {
    if let value = value as? CrashlyticsStackFrame {
      super.writeByte(129)
      super.writeValue(value.toList())
    } else if let value = value as? RecordErrorRequest {
      super.writeByte(130)
      super.writeValue(value.toList())
    } else {
      super.writeValue(value)
    }
  }
}

private class FirebaseCrashlyticsMessagesPigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    FirebaseCrashlyticsMessagesPigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    FirebaseCrashlyticsMessagesPigeonCodecWriter(data: data)
  }
}

class FirebaseCrashlyticsMessagesPigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared =
    FirebaseCrashlyticsMessagesPigeonCodec(
      readerWriter: FirebaseCrashlyticsMessagesPigeonCodecReaderWriter()
    )
}

/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol FirebaseCrashlyticsHostApi {
  func checkForUnsentReports(completion: @escaping (Result<Bool, Error>) -> Void)
  func crash(completion: @escaping (Result<Void, Error>) -> Void)
  func deleteUnsentReports(completion: @escaping (Result<Void, Error>) -> Void)
  func didCrashOnPreviousExecution(completion: @escaping (Result<Bool, Error>) -> Void)
  func recordError(request: RecordErrorRequest, completion: @escaping (Result<Void, Error>) -> Void)
  func log(message: String, completion: @escaping (Result<Void, Error>) -> Void)
  func sendUnsentReports(completion: @escaping (Result<Void, Error>) -> Void)
  func setCrashlyticsCollectionEnabled(
    enabled: Bool,
    completion: @escaping (Result<Bool, Error>) -> Void)
  func setUserIdentifier(identifier: String, completion: @escaping (Result<Void, Error>) -> Void)
  func setCustomKey(key: String, value: String, completion: @escaping (Result<Void, Error>) -> Void)
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class FirebaseCrashlyticsHostApiSetup {
  static var codec: FlutterStandardMessageCodec {
    FirebaseCrashlyticsMessagesPigeonCodec.shared
  }

  /// Sets up an instance of `FirebaseCrashlyticsHostApi` to handle messages through the
  /// `binaryMessenger`.
  static func setUp(
    binaryMessenger: FlutterBinaryMessenger, api: FirebaseCrashlyticsHostApi?,
    messageChannelSuffix: String = ""
  ) {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    let checkForUnsentReportsChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.firebase_crashlytics_platform_interface.FirebaseCrashlyticsHostApi.checkForUnsentReports\(channelSuffix)",
      binaryMessenger: binaryMessenger,
      codec: codec
    )
    if let api {
      checkForUnsentReportsChannel.setMessageHandler { _, reply in
        api.checkForUnsentReports { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      checkForUnsentReportsChannel.setMessageHandler(nil)
    }
    let crashChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.firebase_crashlytics_platform_interface.FirebaseCrashlyticsHostApi.crash\(channelSuffix)",
      binaryMessenger: binaryMessenger,
      codec: codec
    )
    if let api {
      crashChannel.setMessageHandler { _, reply in
        api.crash { result in
          switch result {
          case .success:
            reply(wrapResult(nil))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      crashChannel.setMessageHandler(nil)
    }
    let deleteUnsentReportsChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.firebase_crashlytics_platform_interface.FirebaseCrashlyticsHostApi.deleteUnsentReports\(channelSuffix)",
      binaryMessenger: binaryMessenger,
      codec: codec
    )
    if let api {
      deleteUnsentReportsChannel.setMessageHandler { _, reply in
        api.deleteUnsentReports { result in
          switch result {
          case .success:
            reply(wrapResult(nil))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      deleteUnsentReportsChannel.setMessageHandler(nil)
    }
    let didCrashOnPreviousExecutionChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.firebase_crashlytics_platform_interface.FirebaseCrashlyticsHostApi.didCrashOnPreviousExecution\(channelSuffix)",
      binaryMessenger: binaryMessenger,
      codec: codec
    )
    if let api {
      didCrashOnPreviousExecutionChannel.setMessageHandler { _, reply in
        api.didCrashOnPreviousExecution { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      didCrashOnPreviousExecutionChannel.setMessageHandler(nil)
    }
    let recordErrorChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.firebase_crashlytics_platform_interface.FirebaseCrashlyticsHostApi.recordError\(channelSuffix)",
      binaryMessenger: binaryMessenger,
      codec: codec
    )
    if let api {
      recordErrorChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let requestArg = args[0] as! RecordErrorRequest
        api.recordError(request: requestArg) { result in
          switch result {
          case .success:
            reply(wrapResult(nil))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      recordErrorChannel.setMessageHandler(nil)
    }
    let logChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.firebase_crashlytics_platform_interface.FirebaseCrashlyticsHostApi.log\(channelSuffix)",
      binaryMessenger: binaryMessenger,
      codec: codec
    )
    if let api {
      logChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let messageArg = args[0] as! String
        api.log(message: messageArg) { result in
          switch result {
          case .success:
            reply(wrapResult(nil))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      logChannel.setMessageHandler(nil)
    }
    let sendUnsentReportsChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.firebase_crashlytics_platform_interface.FirebaseCrashlyticsHostApi.sendUnsentReports\(channelSuffix)",
      binaryMessenger: binaryMessenger,
      codec: codec
    )
    if let api {
      sendUnsentReportsChannel.setMessageHandler { _, reply in
        api.sendUnsentReports { result in
          switch result {
          case .success:
            reply(wrapResult(nil))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      sendUnsentReportsChannel.setMessageHandler(nil)
    }
    let setCrashlyticsCollectionEnabledChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.firebase_crashlytics_platform_interface.FirebaseCrashlyticsHostApi.setCrashlyticsCollectionEnabled\(channelSuffix)",
      binaryMessenger: binaryMessenger,
      codec: codec
    )
    if let api {
      setCrashlyticsCollectionEnabledChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let enabledArg = args[0] as! Bool
        api.setCrashlyticsCollectionEnabled(enabled: enabledArg) { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      setCrashlyticsCollectionEnabledChannel.setMessageHandler(nil)
    }
    let setUserIdentifierChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.firebase_crashlytics_platform_interface.FirebaseCrashlyticsHostApi.setUserIdentifier\(channelSuffix)",
      binaryMessenger: binaryMessenger,
      codec: codec
    )
    if let api {
      setUserIdentifierChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let identifierArg = args[0] as! String
        api.setUserIdentifier(identifier: identifierArg) { result in
          switch result {
          case .success:
            reply(wrapResult(nil))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      setUserIdentifierChannel.setMessageHandler(nil)
    }
    let setCustomKeyChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.firebase_crashlytics_platform_interface.FirebaseCrashlyticsHostApi.setCustomKey\(channelSuffix)",
      binaryMessenger: binaryMessenger,
      codec: codec
    )
    if let api {
      setCustomKeyChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let keyArg = args[0] as! String
        let valueArg = args[1] as! String
        api.setCustomKey(key: keyArg, value: valueArg) { result in
          switch result {
          case .success:
            reply(wrapResult(nil))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      setCustomKeyChannel.setMessageHandler(nil)
    }
  }
}
